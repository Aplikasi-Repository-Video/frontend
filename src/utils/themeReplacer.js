// export const replaceThemeClasses = (htmlString) => {
//   const replacements = {
//     'bg-\\[#0f0b1d\\]': 'bg-primary',
//     'text-white': 'text-primary',
//     'bg-gray-800': 'bg-secondary',
//     'text-gray-300': 'text-secondary',
//   }

//   let result = htmlString
//   Object.entries(replacements).forEach(([oldClass, newClass]) => {
//     result = result.replace(new RegExp(oldClass, 'g'), newClass)
//   })

//   return result
// }
